`pomodoro.svg` is user-provided artwork (from `~/Downloads/tomato-podomoro.svg`),
with `stroke-width` bumped from the default (too thin at bar-icon size) to 3.

`pause.svg` is an original hand-drawn shape, no external license.
